# ============================================================================
# financial-prompt-agent — dependências
# ----------------------------------------------------------------------------
# Instale com:  pip install -r requirements.txt
# Provedores de LLM e fontes de dados são de instalação opcional (ver abaixo).
# ============================================================================

# --- Núcleo da aplicação ----------------------------------------------------
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
PyYAML>=6.0

# --- API ---------------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27

# --- Orquestração do agente (LangChain moderno) -----------------------------
langchain>=0.3
langchain-core>=0.3
langgraph>=0.2

# --- Dados / análise --------------------------------------------------------
pandas>=2.2
openpyxl>=3.1          # leitura/escrita de planilhas .xlsx (importação do dataset)
yfinance>=0.2.40       # fonte de dados AO VIVO ("Mercado atual") — cotações com atraso

# --- Interface --------------------------------------------------------------
streamlit>=1.36

# --- Testes -----------------------------------------------------------------
pytest>=8.0

# ============================================================================
# OPCIONAIS — instale apenas o que for usar de fato.
# ----------------------------------------------------------------------------
# Provedores de LLM (carregados de forma lazy; só precisam estar instalados
# quando o provedor correspondente for efetivamente utilizado):
#   pip install langchain-openai
#   pip install langchain-anthropic
#   pip install langchain-google-genai
#   (OpenRouter reutiliza langchain-openai apontando para a base URL do OpenRouter)
#
# Fonte de dados financeiros ao vivo:
#   pip install yfinance
#
# Análise estatística e gráficos publicáveis (PROMPT 12):
#   pip install scipy matplotlib
# ============================================================================
